const { resolve } = require('node:path')

module.exports = {
    config: resolve(__dirname, 'src', 'config', 'database.js'),
    'models-pach': resolve(__dirname, 'src', 'app', 'models'),
    'migrations-pach': resolve(__dirname, 'src', 'database', 'migrations')
}